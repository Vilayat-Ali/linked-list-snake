type Props = {
  username: string;
  review: number;
  message: string;
};

const ReviewCard = ({ username, review, message }: Props) => {
  return (
    <>
      <div>ReviewCard</div>
    </>
  );
};

export default ReviewCard;
