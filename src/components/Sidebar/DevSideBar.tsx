// Libraries
import { useState } from "react";

type Props = {};

const DevSideBar = (props: Props) => {
  // states
  const [reaction, recordReaction] = useState<String>("");
  return <div>DevSideBar</div>;
};

export default DevSideBar;
